import React from 'react';

type Props = {
  data: any;
  activeVideo: number;
  setActiveVideo: (activeVideo: number) => void;
};

function CourseContentList({ data, activeVideo, setActiveVideo }: Props) {
  return <div>CourseContentList</div>;
}

export default CourseContentList;
